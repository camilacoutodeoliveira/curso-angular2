var minhaVar = 'minha variavel'

function minhaFunc(x,y) {
    return x + y;
}

let num = 2;
const PI = 3.14;
var numeros = [1,2,3,4,5]
numeros.map(f=> f * 7);

class MyClass{
    soma(x, y){
       return x + y;
    }
}

var n1 : string = 'camila'
